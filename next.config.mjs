/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/molcristi203.github.io",
    images: {
        unoptimized: true
    }
};

export default nextConfig;
